# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# sensitive files: credentials, etc
# Do NOT commit these to git or store them publicly in Github - these should be kept secret (they are used to identify ourselves to the Google Apps Script APIs).
server/credentials.json
server/token.json

# dependencies
server/node_modules
client/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
client/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
